# 🛡️ 运维与风控改进优化手册

> **核心原则**：先保证不亏，再考虑赚多赚少。

---

## 🚨 一、已实现的风控机制（代码级）

### 1. 策略级熔断机制

**触发条件**：
- ✅ 连续亏损3次 → 暂停开仓
- ✅ 单日亏损超过500USDT → 暂停开仓
- ✅ 人工触发暂停 → 远程控制

**恢复条件**：
- 熔断后24小时自动尝试恢复
- 或人工干预后手动恢复

**代码位置**：`FundingArbitrageBot.java` → `isTradingAllowed()`

---

### 2. 净敞口限制（多空平衡）

**规则**：
- ✅ 净空头/净多头敞口不得超过总仓位的50%
- ✅ 开仓前自动计算对净敞口的影响
- ✅ 超限则拒绝开仓，避免单边裸奔

**为什么重要**：
- 极端行情下（如FTX破产、LUNA归零），全空可能赚，但全多可能爆仓
- 保持中性，赚纯资金费，不赌方向

**代码位置**：`calculateNetExposure()` + `isExposureAcceptable()`

---

### 3. 费率趋势过滤（不接下落的刀）

**规则**：
- ✅ 保存最近20次费率历史
- ✅ 当前费率低于历史均值的70% → 不开仓
- ✅ 防止开在费率下跌通道，刚开仓费率就没了

**数学逻辑**：
```
如果当前费率 < 历史平均费率 × 0.7 → 趋势向下，跳过
```

---

### 4. 极端行情过滤

**触发条件**：
- ✅ 24小时涨跌幅超过 ±10% → 不交易该币种
- ✅ 费率低于 -0.3%（极度异常）→ 不做多
- ✅ 费率绝对值超过 1% → 判定为异常数据

**为什么**：
- 大涨大跌时，费率波动极大，不可持续
- 极端行情下，交易所系统不稳定，容易出问题

---

### 5. 严格的止损止盈

| 风控项 | 参数值 | 说明 |
|-------|-------|-----|
| **单币止损** | 2% | 亏损达到2%强制平仓 |
| **单币止盈** | 3% | 浮盈达到3%主动止盈 |
| **最大回撤** | 10% | 总资金回撤10%全平 |
| **最低余额** | 可配置 | 低于阈值全平 |

> **核心思想**：亏要跑得快，赚要见好就收

---

## 🔧 二、运维监控与异常恢复

### 1. 重试与容错机制

**层级化重试策略**：

| 失败场景 | 重试次数 | 重试间隔 | 降级方案 |
|---------|---------|---------|---------|
| API调用超时 | 3次 | 指数退避 | 跳过本次，下次重试 |
| 开仓失败 | 5次 | 1秒 | 告警通知人工介入 |
| 平仓失败 | 10次 | 3秒 | 持续重试 + 紧急告警 |
| 数据获取失败 | 无限 | 30秒 | 用上次缓存继续运行 |

**代码实现**：`RetryManager.java`

---

### 2. 启动状态恢复

**崩溃恢复流程**：

```
程序启动
    ↓
读取持久化文件中的持仓状态
    ↓
查询交易所实际持仓
    ↓
对比差异：
  ├─ 本地有持仓，实际没有 → 修正本地状态
  ├─ 本地无持仓，实际有 → 恢复持仓记录
  └─ 两边不一致 → 告警通知
    ↓
继续正常运行
```

---

### 3. 内置Web仪表盘

**访问地址**：`http://localhost:8080`

**功能清单**：

| 功能 | 说明 |
|-----|-----|
| 📊 实时状态 | 当前持仓、累计收益、今日结算 |
| 🔘 远程控制 | 一键暂停/恢复交易、一键全平 |
| ⚙️ 参数热更 | 在线修改开仓阈值、止损比例 |
| 📈 费率排行 | 实时显示各币种费率排名 |
| 🔔 告警推送 | 支持飞书/钉钉Webhook |

---

## 🚨 三、交易所宕机与极端行情预案

### 场景1：API完全断开

**应对措施**：
1. ✅ 立即停止所有开仓操作
2. ✅ 后台持续重试连接（每30秒）
3. ✅ 发出最高级别告警（电话/短信）
4. ✅ 连接恢复后，先同步持仓再决定操作

**最坏情况处理**：
- API断开超过4小时 → 人工登录交易所网页端平仓

---

### 场景2：单币种插针爆仓

**应对措施**：
1. ✅ 保证金守护线程每30秒检查一次保证金率
2. ✅ 保证金率 < 10% → 预警
3. ✅ 保证金率 < 5% → 自动从现货划转保证金到合约
4. ✅ 极端情况：自动减仓20%

---

### 场景3：交易所暂停提款/交易

**预防措施**：
1. ✅ 资金分散到3家以上交易所
2. ✅ 单交易所资金不超过总资金的50%
3. ✅ 每周做一次提现测试，确保通道畅通

---

### 场景4：费率突然反转

**应对措施**：
1. ✅ 检测到费率反转（正变负或负变正）
2. ✅ 立即启动平仓流程
3. ✅ 发出告警通知
4. ✅ 记录到异常日志，后续分析

---

## 📊 四、回测与参数优化框架

### 回测系统设计

**数据层**：
```
数据来源：
  ├─ 币安API历史资金费率（2019年至今）
  ├─ 小时级K线数据
  └─ 交易所历史费率统计

存储格式：CSV / Parquet
更新频率：每日自动拉取增量
```

**回测引擎**：
```
参数配置：
  ├─ 开仓阈值：0.05% ~ 0.2%（步长0.01%）
  ├─ 止损比例：1% ~ 5%（步长0.5%）
  ├─ 最大持仓数：2 ~ 10个
  └─ 移仓阈值：0.05% ~ 0.3%

输出指标：
  ├─ 总收益率 / 年化收益率
  ├─ 最大回撤 / 夏普比率
  ├─ 胜率 / 盈亏比
  └─ 交易次数 / 手续费总额
```

**参数寻优**：
- 网格搜索 + 遗传算法
- 过去1年数据训练，过去3个月数据验证
- 避免过拟合

---

## 🔔 五、告警分级体系

### P0级告警（立即响应）

| 触发条件 | 通知方式 | 响应SLA |
|---------|---------|---------|
| 平仓连续失败10次 | 电话 + 短信 + 群@所有人 | 5分钟 |
| 保证金率 < 5% | 电话 + 短信 | 10分钟 |
| 最大回撤触发全平 | 群@所有人 | 30分钟 |
| 程序崩溃退出 | 监控系统告警 | 立即 |

### P1级告警（1小时内响应）

| 触发条件 | 通知方式 |
|---------|---------|
| 开仓失败3次以上 | 群消息@管理员 |
| API断开超过30分钟 | 群消息@管理员 |
| 单日亏损达到300USDT | 群消息@管理员 |

### P2级告警（24小时内处理）

| 触发条件 | 通知方式 |
|---------|---------|
| 连续3次无开仓机会 | 日报统计 |
| 费率异常值检测 | 日报统计 |
| 每日收益报告 | 每日推送 |

---

## 📋 六、日常运维SOP

### 每日检查清单（开盘前）

- [ ] 检查程序运行状态 → `ps aux | grep java`
- [ ] 检查昨晚资金费结算 → 查看日志或仪表盘
- [ ] 检查账户余额和保证金率
- [ ] 检查是否有未处理的告警
- [ ] 查看费率排行，预估今日机会

### 每周检查清单

- [ ] 导出交易记录，做盈亏分析
- [ ] 检查策略参数是否需要调整
- [ ] 备份日志和持久化文件
- [ ] 做一次提现测试（确保通道畅通）

### 每月检查清单

- [ ] 完整月度复盘报告
- [ ] 参数回测验证，决定是否调参
- [ ] 服务器安全更新
- [ ] 灾备演练：模拟程序崩溃恢复

---

## 🌐 七、多交易所与多实例部署

### 部署架构

```
                        ┌─────────────────┐
                        │   负载均衡层     │
                        │  (nginx/haproxy) │
                        └────────┬────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
┌───────▼───────┐       ┌───────▼───────┐       ┌───────▼───────┐
│  Binance实例  │       │   OKX实例     │       │  Bybit实例    │
│  (50%资金)    │       │  (30%资金)    │       │  (20%资金)    │
└───────┬───────┘       └───────┬───────┘       └───────┬───────┘
        │                        │                        │
        └────────────────────────┼────────────────────────┘
                                 │
                        ┌────────▼────────┐
                        │   统一仪表盘     │
                        │  + 数据聚合      │
                        │  + 告警中心      │
                        └─────────────────┘
```

### 配置同步机制

- 主配置文件统一管理
- 修改后自动同步到所有实例
- 支持灰度发布：先更新一个实例验证，再全量

---

## 🚨 八、黑天鹅事件应急预案

### 定义黑天鹅：
- 单日BTC涨跌幅 > 20%
- 交易所暂停服务 > 4小时
- 监管重大政策变化
- 某币种单日归零（如LUNA）

### 自动应对流程：

```
检测到黑天鹅事件
    ↓
立即暂停所有开仓操作
    ↓
检查所有持仓的浮亏情况
    ↓
浮亏单币 > 5% → 立即平仓
    ↓
净敞口 > 30% → 对冲减仓
    ↓
发出最高级别告警，人工介入
    ↓
等待24小时，市场稳定后再恢复
```

---

## 📈 九、性能优化建议

### 当前瓶颈分析

| 操作 | 耗时 | 优化空间 |
|-----|-----|---------|
| 全量持仓查询 | ~200ms | 可异步缓存 |
| 单币种费率查询 | ~50ms | 批量查询 |
| 开仓/平仓 | ~300ms | 可接受 |
| 循环检查间隔 | 10分钟 | 合理 |

### 优化方向

1. ✅ 异步并发查询（已在RetryManager中实现）
2. ✅ 数据缓存（减少API调用）
3. ⏳ WebSocket订阅实时数据（代替轮询）
4. ⏳ 读写分离（历史数据分析不影响实盘）

---

## ✅ 十、风控检查清单（上线前必过）

### 代码层面
- [ ] 所有API调用都有重试和异常处理
- [ ] 开仓前检查：费率、净敞口、熔断、趋势
- [ ] 平仓逻辑完整：止损、止盈、费率降低、紧急全平
- [ ] 异常情况下不会重复下单
- [ ] 所有关键操作有日志记录

### 运维层面
- [ ] 服务器资源足够（CPU < 50%, 内存 < 70%）
- [ ] 网络稳定，延迟 < 100ms
- [ ] 监控系统已配置
- [ ] 告警渠道测试通过
- [ ] 有备用服务器可以快速切换

### 资金层面
- [ ] 仓位大小计算正确
- [ ] 杠杆设置正确
- [ ] 保证金预留充足
- [ ] 资金分散到多个交易所

---

> **最后提醒**：风控永远是第一位的。
>
> 宁可少赚，也不能大亏。活下来，才能复利。
>
> **每月检查一遍这个清单，确保没有遗漏！**

---

**最后更新**：2026-05-15
