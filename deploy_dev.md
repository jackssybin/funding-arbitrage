# 🚀 资金费率套利机器人 - 部署与使用手册

> 💡 写给周先生的内部使用指南
> 目标：7天模拟盘测试，拿到真实数据，发文章更有底气

---

## 📋 目录

1. [快速开始（5分钟跑起来）](#快速开始5分钟跑起来)
2. [项目文件结构详解](#项目文件结构详解)
3. [核心策略逻辑详解](#核心策略逻辑详解)
4. [配置文件参数说明](#配置文件参数说明)
5. [内置Web仪表盘使用说明](#内置web仪表盘使用说明)
6. [模拟盘 vs 实盘切换](#模拟盘-vs-实盘切换)
7. [7天测试计划与记录模板](#7天测试计划与记录模板)
8. [常见问题排查](#常见问题排查)

---

## ⚡ 快速开始（5分钟跑起来）

### 第一步：环境准备

```bash
# 1. 检查Java版本（需要Java 17+）
java -version

# 2. 检查Maven版本（需要Maven 3.6+）
mvn -version
```

### 第二步：编译打包

```bash
cd funding-arbitrage

# 编译并打包（跳过测试）
mvn clean package -DskipTests

# 成功后会在 target 目录下生成 jar 包
ls -lh target/funding-arbitrage-1.0.0.jar
```

### 第三步：配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑配置（先跑模拟盘，不需要填真实API Key）
vim .env
```

**模拟盘最小配置（先跑这个）：**

```env
# ===== 交易所选择 =====
EXCHANGE=binance  # 先用币安测试

# ===== API配置（模拟盘可以先不填真实的） =====
API_KEY=test_key
SECRET_KEY=test_secret

# ===== 策略核心参数 =====
MAX_POSITIONS=3          # 最多同时持仓3个币
POSITION_VALUE_USDT=300  # 每个仓位300USDT（模拟盘可以调小一点）

# ===== 模式选择 =====
SIMULATION_MODE=true     # true=模拟盘，false=实盘

# ===== 仪表盘 =====
DASHBOARD_PORT=8080      # Web仪表盘端口
```

### 第四步：启动程序

```bash
# 启动（后台运行）
java -jar target/funding-arbitrage-1.0.0.jar

# 或者后台运行并输出日志
nohup java -jar target/funding-arbitrage-1.0.0.jar > app.log 2>&1 &
```

### 第五步：验证启动成功

```bash
# 1. 查看日志
tail -f app.log

# 看到类似输出就是成功了：
# ✅ Binance API 连接成功
# 🛡️ 保证金守护线程已启动
# 🌐 仪表盘已启动: http://localhost:8080
# 🚀 机器人启动成功！

# 2. 打开浏览器访问仪表盘
open http://localhost:8080
```

✅ **恭喜，5分钟搞定！接下来就等它自动跑了。**

---

## 📁 项目文件结构详解

```
funding-arbitrage/
├── 📄 pom.xml                          # Maven项目配置（依赖、打包方式）
│
├── 📄 .env.example                     # 环境变量模板（复制为.env）
│
├── 📄 start.bat                        # Windows启动脚本（UTF-8编码）
├── 📄 start.sh                         # Linux/Mac启动脚本
│
├── 📂 src/main/java/com/quant/
│   ├── 📄 FundingArbitrageBot.java     # 【核心】主程序入口、策略主逻辑
│   ├── 📄 Config.java                  # 【配置】所有参数读取、环境变量
│   ├── 📄 BinanceFuturesClient.java    # 【交易所】币安合约API封装
│   ├── 📄 OkxClient.java               # 【交易所】OKX合约API封装
│   ├── 📄 ExchangeClient.java          # 【交易所】统一接口定义
│   ├── 📄 Position.java                # 【持仓】单个币种的持仓状态
│   ├── 📄 SmartOrderExecutor.java      # 【交易】智能下单器（拆单、重试）
│   ├── 📄 AtomicTransactionManager.java# 【交易】事务管理器（原子性保证）
│   ├── 📄 ExchangePrecision.java       # 【交易】精度对齐（防止API报错）
│   ├── 📄 StrategyControl.java         # 【控制】策略启停、参数热更新
│   ├── 📄 StrategyPersistence.java     # 【持久化】状态保存/恢复
│   ├── 📄 StrategyDashboard.java       # 【监控】内置Web仪表盘
│   ├── 📄 RetryManager.java            # 【容错】失败重试、异常恢复
│   └── 📄 MarginGuardian.java          # 【风控】保证金监控、自动划转
│
├── 📂 src/main/resources/
│   └── 📄 logback.xml                  # 日志配置（编码、输出格式）
│
├── 📂 data/                            # 持久化数据目录（运行后自动生成）
│   ├── 📄 strategy_state.json          # 策略状态（持仓、收益、参数）
│   └── 📄 trades_history.csv           # 交易历史记录（开平仓、资金费）
│
└── 📂 logs/                            # 日志目录（运行后自动生成）
    ├── 📄 app.log                      # 当前日志
    └── 📄 app.log.2026-05-15           # 历史日志（按天分割）
```

---

## 🧠 核心策略逻辑详解（v2.5版本）

### 🎯 开仓条件（同时满足才开）

```
条件1：费率绝对值 >= 0.15% （正费率做空，负费率做多）
  ↓
条件2：费率趋势向好（不是下降通道）
  ↓
条件3：净敞口限制（单边不超过50%）
  ↓
条件4：持仓数量 < MAX_POSITIONS（默认3个）
  ↓
条件5：策略没有熔断（没有连续亏损、没有单日超额亏损）
  ↓
✅ 开仓！
```

### 📉 平仓条件（满足任意一个就平）

```
满足任意一个就平仓：

1️⃣ 费率下降：费率绝对值 < 0.03%
2️⃣ 触发止损：浮亏 >= 2% （铁律！亏到就跑）
3️⃣ 触发止盈：浮盈 >= 3% （见好就收）
4️⃣ 策略熔断：连续亏3次 OR 单日亏500U
```

### 💰 资金费结算

```
每8小时自动结算（UTC 00:00 / 08:00 / 16:00）

结算流程：
1. 检查每个持仓的当前费率
2. 计算本次应得资金费 = 仓位名义价值 × 费率
3. 计入总收益
4. 记录到 trades_history.csv
5. 保存到 strategy_state.json

例子：
持仓BTC 1000USDT名义价值
本次费率0.15%
本次收益 = 1000 × 0.15% = 1.5USDT
```

### 🛡️ 8级熔断保护机制

| 级别 | 触发条件 | 动作 |
|-----|---------|------|
| L1 | 单币浮亏2% | 平掉该币种 |
| L2 | 连续亏损3次 | 暂停开仓24小时 |
| L3 | 单日亏损>500USDT | 全平仓，暂停开仓24小时 |
| L4 | 净敞口>50% | 不再开同方向仓位 |
| L5 | 最大回撤>10% | 全平仓，暂停开仓3天 |
| L6 | 保证金率<10% | 预警，准备补保证金 |
| L7 | 保证金率<5% | 自动补保证金（Binance） |
| L8 | API异常/网络中断 | 后台重试队列，恢复后自动同步 |

---

## ⚙️ 配置文件参数详解

### 完整的.env配置说明

```env
# ==========================================
# 交易所选择
# ==========================================
# binance=币安，okx=欧易
EXCHANGE=binance

# ==========================================
# Binance API 配置
# ==========================================
# 模拟盘可以先不填真实的
# 实盘需要开通合约交易权限，不需要提现权限
API_KEY=your_api_key_here
SECRET_KEY=your_secret_key_here

# ==========================================
# OKX API 配置（如果用OKX）
# ==========================================
OKX_API_KEY=your_okx_api_key
OKX_SECRET_KEY=your_okx_secret_key
OKX_PASSPHRASE=your_okx_passphrase
# OKX模拟盘开关（true=模拟盘，false=实盘）
OKX_SIMULATED_TRADING=false

# ==========================================
# 策略核心参数
# ==========================================
# 最大同时持仓币种数（建议3-5个，分散风险）
MAX_POSITIONS=3

# 单个币种的名义价值（USDT）
# 建议：模拟盘100-300，实盘500-1000
POSITION_VALUE_USDT=300

# ==========================================
# 费率阈值（决定开平仓时机）
# ==========================================
# ⚠️ v2.5 版本代码里是硬编码的，这里只是说明
# 开仓阈值：费率绝对值大于这个值才考虑开仓
# 默认 0.0015 = 0.15%
FUNDING_RATE_THRESHOLD=0.0015

# 平仓阈值：费率绝对值小于这个值就平仓
# 默认 0.0003 = 0.03%
FUNDING_RATE_CLOSE_THRESHOLD=0.0003

# ==========================================
# 风控参数
# ==========================================
# 单币止损比例（默认 0.02 = 2%）
STOP_LOSS_PERCENT=0.02

# 最大回撤比例（默认 0.1 = 10%）
MAX_DRAWDOWN_PERCENT=0.1

# 单日最大亏损（USDT）
MAX_DAILY_LOSS=500

# 账户最小余额（低于这个值全平仓）
MIN_BALANCE=500

# ==========================================
# 运行配置
# ==========================================
# 策略检查间隔（毫秒，默认10分钟=600000）
CHECK_INTERVAL_MS=600000

# 资金费率更新间隔（毫秒，默认30分钟=1800000）
RATE_UPDATE_INTERVAL_MS=1800000

# 模拟模式开关（true=模拟下单，false=真实下单）
SIMULATION_MODE=true

# ==========================================
# 仪表盘配置
# ==========================================
# Web仪表盘端口
DASHBOARD_PORT=8080
```

---

## 🖥️ 内置Web仪表盘使用说明

### 访问地址
```
http://你的服务器IP:8080
```

### 仪表盘功能说明

#### 🏠 首页：实时概览
```
┌─────────────────────────────────────────┐
│  📊 运行状态                              │
│  ├─ 策略状态：运行中/已暂停/已熔断       │
│  ├─ 当前持仓：3个                         │
│  ├─ 累计收益：125.5 USDT                 │
│  └─ 今日收益：8.3 USDT                   │
│                                          │
│  💰 当前持仓列表                          │
│  ├─ BTCUSDT  做空  费率0.15%  浮盈+0.8%  │
│  ├─ ETHUSDT  做空  费率0.12%  浮亏-0.3%  │
│  └─ SOLUSDT  做多  费率-0.18% 浮盈+1.2%  │
│                                          │
│  🎮 快捷操作按钮                          │
│  ├─ [暂停交易]  [恢复交易]                │
│  └─ [一键平仓]                            │
└─────────────────────────────────────────┘
```

#### 📈 历史记录页
- 开平仓历史记录
- 每次资金费结算明细
- 每日收益统计
- 收益率曲线

#### ⚙️ 参数配置页
- 在线修改开仓阈值
- 在线修改止损比例
- 在线修改最大持仓数
- 无需重启程序，立即生效

---

## 🎭 模拟盘 vs 实盘切换

### 模拟盘模式（默认）
```env
SIMULATION_MODE=true
```

**模拟盘特点：**
- ✅ 不会下真实订单，不会亏钱
- ✅ 所有逻辑跟实盘完全一致
- ✅ 适合验证策略、熟悉程序
- ✅ API Key可以随便填（但不能为空）

**建议：先跑7天模拟盘，熟悉了再上实盘。**

---

### 实盘模式
```env
SIMULATION_MODE=false
```

**实盘前检查清单（必须逐项确认）：**

- [ ] **API权限检查**
  - 只开通现货和合约交易权限
  - 绝对不要开通提现权限！
  - 建议IP白名单限制

- [ ] **资金准备**
  - 合约账户转入足够的USDT
  - 建议是：MAX_POSITIONS × POSITION_VALUE_USDT × 1.5（留保证金缓冲）
  - 例：3个仓位 × 300U × 1.5 = 1350U

- [ ] **参数调小**
  - 前1个月仓位减半：POSITION_VALUE_USDT=300 → 改成150
  - 最大持仓数：MAX_POSITIONS=3 → 改成2
  - 小心驶得万年船

- [ ] **告警设置**
  - 配置好飞书/钉钉告警
  - 手机能收到实时通知

- [ ] **人工盯盘第一天**
  - 第一天实盘建议盯着点
  - 确认开平仓逻辑正常
  - 确认手续费、资金费计算正确

---

## 📅 7天测试计划与记录模板

### 🎯 测试目标
1. 验证程序稳定性（不崩溃、不漏单）
2. 验证策略逻辑（开平仓、止损止盈是否正常）
3. 拿到真实的模拟收益数据（为写文章攒素材）

---

### 📊 每日记录模板

#### Day 1 - 启动日
```
📅 日期：2026-05-16
⏰ 启动时间：10:00
🔧 配置：
  - 交易所：Binance模拟盘
  - 最大持仓：3个
  - 单仓位：300USDT
  - 开仓阈值：0.15%

📈 当日情况：
  - 检测到的高费率币种：[BTC 0.18%, ETH 0.12%, SOL 0.21%]
  - 开仓次数：2次（BTC, SOL）
  - 平仓次数：0次
  - 资金费结算：还没到时间
  - 当日浮盈浮亏：BTC +0.5%, SOL -0.3%

🐛 遇到的问题：
  - 问题1：xxx
  - 问题2：xxx

💡 明日改进：
  - 调整xxx参数
  - 观察xxx情况
```

---

#### Day 2 - 7
```
📅 日期：2026-05-xx

📈 当日情况：
  - 当前持仓：[币种列表]
  - 开仓次数：x次
  - 平仓次数：x次
  - 资金费结算：x USDT
  - 累计收益：x USDT
  - 最大回撤：x%

📊 7天汇总：
  - 总开仓次数：x次
  - 总平仓次数：x次
  - 总资金费收益：x USDT
  - 买卖盈亏：x USDT
  - 总收益：x USDT
  - 收益率：x%
  - 胜率：x%
  - 最大回撤：x%

✅ 测试结论：
  - 策略是否有效：是/否
  - 哪些地方需要优化：xxx
  - 实盘需要注意什么：xxx
```

---

### 💡 测试期间重点观察这5件事

1. **开仓是否准确？**
   - 是不是费率到0.15%才开？
   - 会不会乱开仓？

2. **止损止盈是否坚决？**
   - 亏到2%有没有平？
   - 赚3%有没有止盈？
   - 会不会人为干预？

3. **费率下降会不会及时平？**
   - 费率降到0.03%以下有没有平？

4. **程序稳不稳定？**
   - 会不会崩溃？
   - 内存会不会泄漏？
   - 网络断了会不会自动重连？

5. **真实收益怎么样？**
   - 资金费收益多少？
   - 买卖盈亏多少？
   - 手续费吃掉了多少？
   - 最后净收益多少？

---

## 🔍 常见问题排查

### ❌ 问题1：启动报错，连不上API

**症状：**
```
API连接失败，请检查API Key
```

**解决方案：**
1. 确认API Key和Secret正确
2. 确认服务器能访问交易所（国内服务器可能需要代理）
3. 模拟盘模式下，随便填个字符串就行，不影响运行

---

### ❌ 问题2：跑了一天没开仓

**原因：**
很正常！0.15%的费率门槛不算低，不是每天都有机会。

**解决方案：**
1. 耐心等，好机会是等出来的
2. 可以临时把阈值降到0.10%测试一下开仓逻辑
3. 确认一下费率数据有没有正常获取

---

### ❌ 问题3：Web仪表盘打不开

**排查步骤：**
1. 确认端口8080没被占用：`netstat -an | grep 8080`
2. 确认防火墙开了8080端口
3. 看日志里有没有启动成功的提示：`仪表盘已启动: http://localhost:8080`

---

### ❌ 问题4：中文乱码

**Windows解决方案：**
1. 用 start.bat 启动（已经配置了UTF-8参数）
2. 或者手动启动：
```bash
java -Dfile.encoding=UTF-8 -jar target/funding-arbitrage-1.0.0.jar
```

---

### ❌ 问题5：状态恢复异常

**症状：重启后持仓数据不对**

**解决方案：**
1. 删除 data/strategy_state.json
2. 重新启动，程序会从交易所同步真实持仓
3. 第一次启动会自动校准

---

## 📞 遇到问题怎么办？

1. **先看日志**：logs/app.log 里有详细的错误信息
2. **检查配置**：确认.env里的参数都对
3. **看Git历史**：之前的版本有没有类似问题
4. **简化问题**：先恢复默认参数，定位问题点

---

## 🎉 最后一句

这个程序我们已经打磨了好几个版本，核心逻辑很稳。

7天测试期间，不用天天盯着，每天早晚各看一次就行。

拿到真实数据了，发文章的时候更有底气，读者也更信服。

**祝你测试顺利，7天后有个漂亮的收益曲线！🚀**

---

**文档版本**：v2.5
**最后更新**：2026-05-15
